# Webhook 全链路实盘验证报告（v15.5.13-prod-gate）

日期: 2026-07-22  
版本: `v15.5.13-prod-gate`  
路径: **真实** `POST http://127.0.0.1:5003/webhook`（`secret` 字段，未走 `place_market_order` 旁路）

## 结论

**全链路真实验证通过。**  
开仓 → TP1/TP2 + 呼吸止损 → 12 分钟稳定观察 → `CLOSE_QUICK_EXIT` 平仓 → 账本清零，**未进入「未登记来源」接管分支**。

---

## 时间线（UTC）

| 时间 | 事件 | 关键数值 |
|------|------|----------|
| 10:49:41 | 第1次 LONG（qty=0.012, sl_adj≈0.67→0.008） | **失败** `-4164` 名义&lt;20 |
| 10:51:28 | 第2次 LONG | 空仓短时去重 **忽略**（5min） |
| 10:55:02 | 第3次误发旧 payload | 时序去重丢弃 |
| **10:57:48** | **V4 LONG webhook HTTP 200** | `secret` 鉴权通过 · reason=`WEBHOOK_E2E_LIVE_TEST_V4` |
| 10:57:57 | **市价开仓成功** | LONG **0.02** ETH @ **1929.60** · 名义≈**38.6 USDT** |
| 10:58:04 | 呼吸止损已挂 | **SL=1908.60** · `open_atr=13.9984` · `initial_stop=1908.60` |
| 10:58:23 | 盘口确认 | TP1 **1953.73×0.006** · TP2 **1969.83×0.006** · STOP **1908.6×0.02** |
| 10:59:15–11:11:29 | 观察窗口 ~12min | 仓位/止损/TP **全程稳定**（见下） |
| **11:11:30** | **CLOSE_QUICK_EXIT webhook HTTP 200** | reason=`WEBHOOK_E2E_LIVE_TEST_CLOSE` |
| 11:11:34 | 市价平仓成功 | SELL 0.02 |
| 11:11:37 | 账本清零 | watched/sl/atr=0 · monitoring=false |
| 11:11:50 | 验收 | **FLAT_OK** · orders=0 · algos=0 |

---

## 重点核对：信号来源识别

| 检查项 | 结果 |
|--------|------|
| 走真实 `/webhook` + `secret` | ✅ |
| `last_tv_signal.reason` = `WEBHOOK_E2E_LIVE_TEST_V4` | ✅ |
| 出现「未登记来源仓位」接管 | ❌ **未出现** |
| `monitoring=true` 由开仓路径点亮 | ✅ |

---

## 观察窗稳定性（10:59–11:11 UTC）

采样文件: `logs/webhook_e2e_observe.txt`

- 仓位始终 `0.02` @ `1929.60`
- 交易所止损始终 `[1908.6]`（无 1886↔1910 类振荡）
- TP 始终 `[1953.73, 1969.83]`（无撤挂抖动）
- `open_atr` 锁定 `13.9984`
- `hits` 计数为滚动日志噪声（含历史 cancel 字样），**盘口止损价未变**
- ⚠️ 观察中钉钉去重日志出现两次「TP未成交转雷达」告警（11:03 / 11:08），但同期采样显示 TP1/TP2 **仍在盘口**、止损未撤；属超时告警噪声，**未造成核武撤挂或仓位异常**

---

## 平仓归因

日志原文：

```
🧹 反转保护：WEBHOOK_E2E_LIVE_TEST_CLOSE | TV档位 R3 | TV ATR 14.00 | TV价 1928.01
[市价平仓成功] SELL 0.02 ETHUSDT
呼吸/防线账本已清零 | 🧹 反转保护：WEBHOOK_E2E_LIVE_TEST_CLOSE ...
```

- ✅ 归因为 **反转保护**，非「止损平仓」
- ℹ️ 关闭路径 reason 字符串仍带内部兼容编号 `R3`（不影响 RISK20 算仓；开仓 sizing 文案为 RISK20_NOTIONAL5 / 5x）
- 二次 reduceOnly 报 `-2022`：仓已平后的幂等重试，可忽略

---

## 前置失败（测试构造问题，非生产主路径缺陷）

1. `qty=0.012` + TV止损距≈1.0×ATR → `sl_adj≈0.67` → 实下 0.008 → 名义&lt;20  
2. 失败后 5 分钟内同向空仓重复开仓被 `_is_duplicate_flat_entry` 忽略（`SAME_DIR_DEDUP_SEC=300`）  
3. 复用旧 payload / 同 bar 缺 seq → 时序去重  

V4 用 `qty=0.02` + `stop_loss=price−1.5×ATR`（`sl_adj=1`）+ 唯一 `bar_index` + 价差&gt;0.15% 后一次成功。

---

## 异常清单（本轮）

| 项 | 是否阻断 | 说明 |
|----|----------|------|
| 开仓前试探失败 | 否 | 构造/去重，已在 V4 绕过 |
| 平仓 -2022 | 否 | 已平后二次 reduceOnly |
| 未登记接管 | 无 | 本轮 webhook 路径未触发 |
| 止损振荡 / TP 核武 | 无 | 观察窗盘口稳定 |

---

## 收官声明

本次通过**真实 webhook 端点**完成最小金额全生命周期验证，执行层（鉴权 → 先平后开 → RISK20 算仓 → TP1/TP2 → 呼吸止损 → 反转平仓 → 状态清零）**正式确认通过**，可作为与 Gemini 同类测试的对照证据。
